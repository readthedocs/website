version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
    nodejs: "20"
  commands:
    - pip install poetry
    - poetry install
    - npm ci
    - poetry run npm run build-html
    - mkdir -p _readthedocs/html/
    - cp -r output/* _readthedocs/html/
